[pytest]
# Python path configuration - includes current directory and parent directory
# This allows tests to import modules from both the frontend and shared directories
pythonpath = . ..

# Additional pytest options
# -p nicegui.testing.user_plugin: Loads the NiceGUI testing plugin for UI component testing
# -p env: Loads the pytest-env plugin for environment variable management
addopts = -p nicegui.testing.user_plugin -p env

# Asyncio mode configuration
# auto: Automatically detects and handles async test functions
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function

# Environment variables for test execution
# APP_STORAGE_SECRET: Required secret key for NiceGUI session storage during testing
env = 
    D:APP_STORAGE_SECRET=test
